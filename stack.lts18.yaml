resolver: lts-18.28
packages:

extra-deps:

extra-package-dbs: []

flags:
  nejla-common:
    new-singletons: false

nix:
  packages:
    - zlib
    - postgresql

docker:
  enable: false
  image: nejla/haskell-build
